import React from "react";
import Header from "../../Components/Header";
const SamplePresenter = (props) => {
  return (
    <>
      <Header type={-1} />
    </>
  );
};
export default SamplePresenter;
